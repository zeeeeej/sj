###########################################################

包含build、config、driver、src、libs、samples等目录

1.build/
    t23/patch/  kernel的patch
    kiva_env_setup.sh    环境变量配置脚本

2.config/
    uvc.attr uvc2.attr uvc.config 
    根据实际情况修改uvc.config配置参数
    可配置sensor name、i2c addr、分辨率、帧率等

    isvp_pike_kiva_defconfig #kernel配置文件(可根据项目需求适当添加或者裁减配置)

2.driver/
    usbcamera驱动
    
4.src/
    应用程序相关文件

5.libs/
    应用程序依赖库

6.samples/
    应用程序实例

###########################################################


######################## 编译流程 #########################
一、编译内核
1.在内核根目录中打patch(位于build/t23/patch):
	patch -p1 < T23N_uvc_bulk.patch

2.使用/config目录下的 isvp_pike_kiva_defconfig 内核配置编译内核:


3.编译整个固件

二、编译驱动
1.修改build/t23/kiva_env_setup.sh中 KIVA_ENV_PLATFORM 指向的实际使用的os路径

2.配置环境变量：
	source build/t23/kiva_env_setup.sh

3.进入driver/ucamera_driver目录编译usbcamera驱动:
	make

三、编译应用程序
1.将libs/t23_sdk/中的头和.a库文件替换为当前release版本中的头和.a库文件
2.进入samples编译应用程序sample_ucamera
3.打开uvc.config文件配置当前环境的sensor name、i2c addr、分辨率、帧率、是否直通模式
4.将uvc.config、uvc.attr和uvc2.attr放入global_config.h文件中所指定的目录下

四、程序启动脚本参考

insmod usbcamera.ko

./sample_ucamera &

## 新加应用 485 摄像头的程序，使用 Makefile.rst，使用 sample_camera_rst.c

* 选 clone https://gitee.com/jmdvirus/jklibs 切换到 dev 分支
* 部分应用依赖这个分支中的部分文件，以源码形式使用

###########################################################
